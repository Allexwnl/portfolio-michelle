[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA-fallback: alle routes naar index.html (behalve de functions).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
